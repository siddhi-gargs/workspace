const input = 456856;
let number = "" + input;
let sizeNumber = number.length;
sizeNumber--;
for (let j = sizeNumber ; j >= 1 ; j-- ) {
    let each_num = + number[j]
        // console.log (each_num);  
        // console.log(typeof each_num); 
    console.log(+ number[j]);w
}